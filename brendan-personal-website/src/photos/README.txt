Drop landscape photos here as YYYY-MM-DD-short-caption.jpg (or .jpeg/.png/.webp).
Export at 2000px or less on the long edge, ideally under ~500KB each.
The photos page reads this folder at build time; the caption comes from the filename.
